{
  "name": "ref-nftmark-pet-proj",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ref-nftmark-pet-proj",
    "create-local": "graph create --node http://localhost:8020/ ref-nftmark-pet-proj",
    "remove-local": "graph remove --node http://localhost:8020/ ref-nftmark-pet-proj",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ref-nftmark-pet-proj",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.44.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
